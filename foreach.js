const numArray = [3, 6, 9, 12, 15];

numArray.forEach (
    n => {
        console.log(n *  2);
    }
);

const jpCities = ["tokyo", "kyoto", "osaka", "fukuoka", "sapporo"];

jpCities.forEach (
    jpCity => {
        console.log("City in Japan: ", jpCity);
    }
);